# Synthetic examples

All identities, addresses, accounts, resources, and events here are fictional evaluation fixtures. They are not production evidence. The M365 and OCSF examples demonstrate supported field shapes, not complete vendor/schema coverage. Declare partial or unknown source coverage when importing them.

`research-method.txt` is a short illustrative method, suitable for testing an explicitly approved `draft_pack` request when an external model is configured. It is not the uploaded Paper2Agent manuscript.
